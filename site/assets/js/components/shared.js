export function chrome(section,page){return `<div class="brand-lockup"><div class="brand-mark">AK9I</div><div class="brand-divider"></div><div class="brand-endorsement">A Brawner Group Company<br>Train. Certify. Deploy. Protect.</div></div><div class="slide-meta"><span>${section}</span><span class="meta-rule"></span><span>${page}</span></div><div class="slide-footer"><span>AK9I OPERATING SYSTEM RECOMMENDATION</span><span class="footer-line"></span><span>${page}</span></div>`}
export function sectionButton(n,t,d,target){return `<button type="button" class="section-link" data-go="${target}"><div class="number">${n}</div><h3>${t}</h3><p>${d}</p><span class="cue">Go to recommendation</span></button>`}
export function detailButton(n,t,d,target){return `<button type="button" class="detail-link" data-detail-target="${target}"><div class="number">${n}</div><h3>${t}</h3><p>${d}</p><span class="cue">Open detail</span></button>`}
export function textAction(n,t,d,target){return `<button type="button" class="text-action" data-detail-target="${target}"><div class="n">${n}</div><div><h3>${t}</h3><p>${d}</p></div></button>`}

export function stageSlide(id,num,stage,title,sub,color,tags,actions){
  return `<section class="slide" id="${id}" data-main="true" data-main-num="${num}">${chrome('CUSTOMER PIPELINE — '+stage.toUpperCase(),String(num).padStart(2,'0'))}<div class="slide-inner"><p class="eyebrow">${stage}</p><h1 class="headline compact">${title}</h1><p class="subheadline">${sub}</p><div class="stage-rec-layout"><div class="stage-visual"><div class="stage-orbit"><div class="stage-core" style="--stage-color:${color}"><strong>${stage}</strong></div>${tags.map((x,i)=>`<div class="stage-tag t${i+1}">${x}</div>`).join('')}</div></div><div class="stage-actions">${actions.map((a,i)=>textAction(i+1,a[0],a[1],a[2])).join('')}</div></div></div></section>`;
}
