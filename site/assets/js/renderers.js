import {chrome} from './components/shared.js';

export function renderDetails(details, slideTitles){
  return details.map(d=>`<section class="slide detail-slide" id="${d.id}" data-detail="true" data-origin="${d.origin}">${chrome('LINKED DETAIL',d.code)}<div class="slide-inner"><div class="detail-header"><div><p class="eyebrow">${slideTitles[d.origin]}</p><h1 class="headline">${d.title}</h1></div><div class="detail-code">${d.code}</div></div><div class="detail-grid ${d.grid==='four'?'four':''}">${d.columns.map(c=>`<article class="detail-panel"><h3>${c.title}</h3><ul>${c.bullets.map(b=>`<li>${b}</li>`).join('')}</ul></article>`).join('')}</div><div class="detail-nav">${d.prev?`<button type="button" class="adjacent" data-go="${d.prev}">← Prior detail</button>`:''}<button type="button" class="return" data-return="${d.origin}">↩ Return to recommendation</button>${d.next?`<button type="button" class="adjacent" data-go="${d.next}">Next detail →</button>`:''}</div></div></section>`).join('');
}

export function renderSources(){
  return `<section class="slide" id="sources">${chrome('SOURCES','S')}<div class="slide-inner"><p class="eyebrow">Sources and Notes</p><h1 class="headline compact">Sources and Implementation Notes.</h1><div class="sources-list"><div class="source-row"><strong>Peter F. Drucker</strong><span>The organization quote is credited to Drucker’s 1998 essay <a href="https://www.forbes.com/forbes/1998/1005/6207152a.html" target="_blank" rel="noopener">“Management’s New Paradigms”</a>.</span></div><div class="source-row"><strong>Small Business Flight Plan</strong><span>The six-part airplane language is based on the <a href="https://businessmadesimple.com/flight-plan/" target="_blank" rel="noopener">official Business Made Simple Small Business Flight Plan created by Donald Miller</a>.</span></div><div class="source-row"><strong>AK9I Brand Standards</strong><span>Command Navy, Signal Gold, Steel Blue, Slate, Field White, Graphite, typography hierarchy, brand architecture, asset control, uniforms, release review, and vendor continuity follow the AK9I Brand Standards developed for this engagement.</span></div><div class="source-row"><strong>AK9I organizational materials</strong><span>The functional responsibilities and employee roster reflect the organizational chart, job-alignment work, project transcripts, and employee information supplied for the engagement.</span></div><div class="source-row"><strong>Interactive behavior</strong><span>Organization assignments save in localStorage on the presenting browser. No selections are transmitted by this presentation.</span></div></div><div class="detail-nav"><button type="button" class="return" data-return="s1">↩ Return to recommendation</button></div></div></section>`;
}
